namespace $namespace$
{
    using System;
    using System.Diagnostics;

    /// <summary>
    /// Class for running application.
    /// Structure made for easy further using in integration tests.
    /// </summary>
    public class Application
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="Application"/> class.
        /// </summary>
        /// <param name="serviceProvider"></param>
        public Application(IServiceProvider serviceProvider)
        {
            ServiceProvider = serviceProvider;
        }

        /// <summary>
        /// Built service provider for application.
        /// </summary>
        public IServiceProvider ServiceProvider { get; }

        /// <summary>
        /// In this method you can run different background services: kafka consumers etc.
        /// </summary>
        public Application RunServices()
        {
            // ILog log = Logger.Create(typeof(Program));
            // log.Info("Service success started");
            return this;
        }

        /// <summary>
        /// In this method you can add health checks for application.
        /// </summary>
        public Application StartHealthChecks()
        {
            return this;
        }

        /// <summary>
        /// Waiting process for exit.
        /// </summary>
        public void WaitForExit()
        {
            Process.GetCurrentProcess()
                .WaitForExit();
        }
    }
}
